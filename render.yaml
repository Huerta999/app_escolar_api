services:
  - type: web
    name: app_escolar_api
    env: python
    pythonVersion: 3.12.3
    rootDir: app_escolar_api
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn app_escolar_api.wsgi:application
